@model IEnumerable<StockCafeteria.Commande>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fournisseur.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SommeCommande)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateCommande)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fournisseur.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SommeCommande)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateCommande)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCommande }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCommande }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCommande })
        </td>
    </tr>
}

</table>
