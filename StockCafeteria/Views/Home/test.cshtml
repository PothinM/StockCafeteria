
@{
    ViewBag.Title = "test";
}

<body ng-app="app">
    <div class="container">
        <div ng-controller="mainController">

            @*<input type="text" ng-model="message" placeholder="Enter food" />
            <p>Eh ! {{message}}!</p>*@
            <center><h1>Cafeteria</h1></center>
            <div class="col-lg-6">
                <div class="col-lg-10">
                    <p style="font-size:medium;"><b>Commandes</b></p>
                </div>
                <div class="col-lg-1">
                    <button class="btn btn-default"><span class="glyphicon glyphicon-plus"></span></button>
                </div>
                <table class="table table-responsive table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Libellé</th>
                            <th>Prix</th>
                        </tr>
                    </thead>
                    <tr ng-repeat="to in TypeObjets">
                        <td>{{to.Libelle}}</td>
                        <td>{{to.PrixVente}}</td>
                    </tr>
                </table>
            </div>

        </div>
    </div>
</body>